export const BASE_URL = "http://localhost:3444";

const API_VERSION = "/api/v1";

export const URLS = {
  AUTH: API_VERSION + "/auth",
  ORDERS: API_VERSION + "/orders",
  PRODUCTS: API_VERSION + "/products",
};
